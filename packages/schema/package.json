{
  "name": "@carpeos/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Machine-readable CarpeOS v1 schemas and conformance helpers.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/packages/schema/src/index.js",
  "types": "./dist/packages/schema/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/packages/schema/src/index.d.ts",
      "default": "./dist/packages/schema/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm check:validators && tsc",
    "check:validators": "node scripts/generate-standalone-validators.mjs --check",
    "generate:validators": "node scripts/generate-standalone-validators.mjs",
    "typecheck": "pnpm check:validators && tsc -p tsconfig.typecheck.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
