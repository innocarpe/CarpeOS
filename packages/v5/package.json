{
  "name": "@carpeos/v5",
  "version": "0.0.0",
  "private": true,
  "description": "CarpeOS 5.0 offline draft-only contracts: redaction, EvidencePack, reducer, provider fakes, telemetry admission, evaluation.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run --passWithNoTests",
    "m0:recompute": "node scripts/m0-recompute.mjs",
    "m0:check": "node scripts/m0-recompute.mjs --check-only",
    "cost:experiment:dry": "node scripts/live-cost-experiment.mjs --dry-run",
    "cost:experiment": "node scripts/live-cost-experiment.mjs --allow-network",
    "m8:decision": "pnpm build && node scripts/m8-decision.mjs",
    "verify:offline": "node scripts/verify-offline.mjs"
  }
}
