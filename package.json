{
  "name": "@carpeos/root",
  "version": "0.0.0",
  "private": true,
  "description": "Public monorepo for CarpeOS, a personal knowledge operating system for AI-assisted work.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=22.22.0",
    "pnpm": ">=11.16.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "check": "pnpm format:check && pnpm lint && pnpm build && pnpm typecheck && pnpm test && pnpm public-boundary",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "public-boundary": "node scripts/check-public-boundary.mjs",
    "test": "pnpm -r --if-present test && node --test scripts/test/*.test.mjs",
    "typecheck": "pnpm -r --if-present typecheck",
    "install:local": "node scripts/install-local.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/node": "^22.18.6",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
